import React from 'react';
import ProjectsWindow from '@/components/windows/ProjectsWindow';

const MyProjectsContent: React.FC<any> = props => {
  return <ProjectsWindow {...props} />;
};

export default MyProjectsContent;
